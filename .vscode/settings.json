{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "DuckDB",
            "database": "${workspaceFolder:stock}/data/instockdb.duckdb",
            "name": "2"
        }
    ]
}